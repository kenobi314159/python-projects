# CI/CD Graph generator

This project implements an algorithm for generating a planar graph layout based on definition of tasks flow.
The tasks flow is specified using hierarchical "serial" and "parallel" structure used in CI/CD pipelines.
Specifically, the definition is taken from a json structure in an input file.
